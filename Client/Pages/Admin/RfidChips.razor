@page "/admin-lc/rfid-chips"
@using VitrineFr.Services
@using VitrineFr.Models
@using System.Net.Http.Headers
@using System.Security.Cryptography
@using System.Text
@inject HttpClient Http
@inject AuthService AuthService
@inject NavigationManager Navigation
@inject IJSRuntime JS

<div class="min-h-screen bg-gray-50 py-8 px-4">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="mb-8">
            <button @onclick="NavigateToDashboard"
                    class="lc-btn lc-btn-secondary inline-flex items-center mb-4">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
                Retour
            </button>

            <div class="flex justify-between items-center flex-wrap gap-4">
                <div>
                    <h1 class="lc-heading-1">Gestion RFID</h1>
                </div>
                <div class="flex gap-3 flex-wrap">
                    <button @onclick="OpenAddManualModal" class="lc-btn lc-btn-primary inline-flex items-center">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                        </svg>
                        Ajouter puce manuellement
                    </button>
                    <a href="/admin-lc/import-chips" class="lc-btn lc-btn-primary inline-flex items-center">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                        </svg>
                        Importer Excel
                    </a>
                    <button @onclick="RefreshData" class="lc-btn lc-btn-secondary inline-flex items-center">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                        </svg>
                        Actualiser
                    </button>
                </div>
            </div>
        </div>

        <!-- Filtres -->
        <div class="bg-white rounded-xl shadow-sm p-6 mb-6">
            <div class="grid md:grid-cols-3 gap-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Statut</label>
                    <select @bind="selectedStatus" @bind:after="FilterData"
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                        <option value="">Tous</option>
                        <option value="EN_TRANSIT">🚚 En transit</option>
                        <option value="EN_ATELIER">🔧 En atelier</option>
                        <option value="EN_STOCK">📦 En stock</option>
                        <option value="EN_LIVRAISON">🚛 En livraison</option>
                        <option value="LIVREE">✅ Livrée</option>
                        <option value="ACTIVE">🟢 Active</option>
                        <option value="RETOUR_SAV">🔄 Retour SAV</option>
                        <option value="RECEPTION_SAV">📥 Réception SAV</option>
                        <option value="REMPLACEE">🔁 Remplacée</option>
                        <option value="ARCHIVEE">📁 Archivée</option>
                        <option value="INACTIVE">⚫ Inactive</option>
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Recherche UID</label>
                    <input type="text" @bind="searchUid" @bind:after="FilterData"
                           placeholder="Rechercher par UID..."
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
                </div>

                <div class="flex items-end">
                    <button @onclick="ClearFilters"
                            class="w-full px-4 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">
                        Réinitialiser filtres
                    </button>
                </div>
            </div>
        </div>

        <!-- Tableau -->
        @if (isLoading)
        {
            <div class="flex justify-center items-center py-20">
                <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
            </div>
        }
        else if (filteredChips.Count == 0)
        {
            <div class="bg-white rounded-xl shadow-sm p-12 text-center">
                <div class="text-gray-400 text-5xl mb-4">📋</div>
                <p class="text-gray-600">Aucune puce trouvée</p>
            </div>
        }
        else
        {
            <div class="bg-white rounded-xl shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h2 class="text-lg font-bold text-gray-900">
                        Puces RFID (@filteredChips.Count)
                    </h2>
                </div>

                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ChipId</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Client</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Site</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Statut</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            @foreach (var chip in filteredChips)
                            {
                                <tr class="hover:bg-gray-50 cursor-pointer" @onclick="() => ViewChipDetails(chip.Id)">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="text-sm font-mono font-semibold text-gray-900">@chip.ChipId</span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="text-sm text-gray-600">@(chip.CustomerName ?? "-")</span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="text-sm text-gray-600">@(chip.SiteName ?? "-")</span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="@GetStatusBadgeClass(chip.Status)">
                                            @GetStatusLabel(chip.Status)
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm">
                                        <button @onclick:stopPropagation @onclick="() => ViewChipDetails(chip.Id)"
                                                class="text-blue-600 hover:text-blue-900 font-medium">
                                            Détails
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    </div>
</div>

<!-- Modal Ajout manuel -->
@if (showAddManualModal)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 p-4" @onclick="CloseAddManualModal">
        <div class="bg-white rounded-xl shadow-2xl max-w-md w-full" @onclick:stopPropagation>
            <div class="bg-blue-50 border-b border-blue-200 p-6">
                <h3 class="text-2xl font-bold text-gray-900">Ajouter une puce manuellement</h3>
            </div>
            <div class="p-6">
                @if (!string.IsNullOrEmpty(addManualError))
                {
                    <div class="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm">
                        @addManualError
                    </div>
                }
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-2">UID de la puce (depuis NFCTools)</label>
                    <input type="text" @bind="manualUid" placeholder="Ex: 04:AB:CD:12:34:56:78"
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 font-mono"
                           @onkeyup="HandleManualUidKeyUp" />
                    <p class="text-xs text-gray-500 mt-1">Format: 14 ou 20 caractères hexadécimaux (7 ou 10 bytes)</p>
                </div>
            </div>
            <div class="bg-gray-50 border-t border-gray-200 p-6 flex justify-between">
                <button @onclick="CloseAddManualModal" class="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">
                    Annuler
                </button>
                <button @onclick="AddManualChip" disabled="@isAddingManual" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition disabled:opacity-50">
                    @if (isAddingManual) { <span>Ajout en cours...</span> } else { <span>Ajouter</span> }
                </button>
            </div>
        </div>
    </div>
}

<!-- Modal Détail avec Actions -->
@if (showDetailModal && selectedChip != null)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 p-4" @onclick="CloseDetailModal">
        <div class="bg-white rounded-xl shadow-2xl max-w-2xl w-full max-h-[90vh] overflow-y-auto" @onclick:stopPropagation>
            <div class="bg-blue-50 border-b border-blue-200 p-6">
                <div class="flex items-start justify-between">
                    <div>
                        <h3 class="text-2xl font-bold text-gray-900">Détail Puce</h3>
                        <p class="text-sm text-gray-600 mt-1">@selectedChip.ChipId</p>
                        <div class="mt-2">
                            <span class="@GetStatusBadgeClass(selectedChip.Status)">
                                @GetStatusLabel(selectedChip.Status)
                            </span>
                        </div>
                    </div>
                    <button @onclick="CloseDetailModal" class="text-gray-400 hover:text-gray-600 transition">
                        <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>
            </div>
            <div class="p-6">
                <div class="grid md:grid-cols-2 gap-6 mb-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">ChipId</label>
                        <p class="text-lg font-mono text-gray-900">@selectedChip.ChipId</p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">UID</label>
                        <p class="text-lg font-mono text-gray-900">@selectedChip.Uid</p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Statut</label>
                        <p class="text-lg">
                            <span class="@GetStatusBadgeClass(selectedChip.Status)">
                                @GetStatusLabel(selectedChip.Status)
                            </span>
                        </p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Créée le</label>
                        <p class="text-lg text-gray-900">@selectedChip.CreatedAt.ToString("dd/MM/yyyy HH:mm:ss")</p>
                    </div>
                </div>

                <!-- Boutons d'action selon l'état -->
                <div class="bg-gray-50 rounded-lg p-4 mb-6">
                    <h4 class="text-sm font-semibold text-gray-700 mb-3">Actions disponibles</h4>
                    <div class="flex flex-wrap gap-2">
                        <!-- Bouton Encoder (toujours visible, grisé si déjà encodé) -->
                        <button @onclick="() => ExecuteAction(ActionEncode)"
                                disabled="@selectedChip.IsKeyProgrammed"
                                class="px-3 py-2 text-sm rounded-lg transition @(selectedChip.IsKeyProgrammed ? "bg-gray-300 text-gray-500 cursor-not-allowed" : "bg-blue-600 text-white hover:bg-blue-700")">
                            🔧 Encoder la puce
                        </button>

                        @if (selectedChip.Status == "EN_TRANSIT")
                        {
                            <button @onclick="() => ExecuteAction(ActionReceiveFromSupplier)" class="px-3 py-2 bg-green-600 text-white text-sm rounded-lg hover:bg-green-700 transition">
                                ✅ Recevoir du fournisseur
                            </button>
                        }
                        @if (selectedChip.Status == "EN_ATELIER")
                        {
                            <!-- Encoder button already shown above -->
                        }
                        @if (selectedChip.Status == "EN_STOCK")
                        {
                            <button @onclick="() => ExecuteAction(ActionShipToClient)" class="px-3 py-2 bg-purple-600 text-white text-sm rounded-lg hover:bg-purple-700 transition">
                                🚛 Expédier au client
                            </button>
                        }
                        @if (selectedChip.Status == "EN_LIVRAISON")
                        {
                            <button @onclick="() => ExecuteAction(ActionConfirmDelivery)" class="px-3 py-2 bg-green-600 text-white text-sm rounded-lg hover:bg-green-700 transition">
                                ✅ Confirmer livraison
                            </button>
                        }
                        @if (selectedChip.Status == "LIVREE")
                        {
                            <button @onclick="() => ExecuteAction(ActionAssignToControlPoint)" class="px-3 py-2 bg-blue-600 text-white text-sm rounded-lg hover:bg-blue-700 transition">
                                📍 Assigner à point de contrôle
                            </button>
                        }
                        @if (selectedChip.Status == "ACTIVE")
                        {
                            <button @onclick="() => ExecuteAction(ActionRequestSav)" class="px-3 py-2 bg-orange-600 text-white text-sm rounded-lg hover:bg-orange-700 transition">
                                🔄 Demander SAV
                            </button>
                            <button @onclick="() => ExecuteAction(ActionDeactivate)" class="px-3 py-2 bg-gray-600 text-white text-sm rounded-lg hover:bg-gray-700 transition">
                                ⚫ Désactiver
                            </button>
                        }
                        @if (selectedChip.Status == "RETOUR_SAV")
                        {
                            <button @onclick="() => ExecuteAction(ActionReceiveSav)" class="px-3 py-2 bg-green-600 text-white text-sm rounded-lg hover:bg-green-700 transition">
                                📥 Confirmer réception SAV
                            </button>
                        }
                        @if (selectedChip.Status == "RECEPTION_SAV")
                        {
                            <button @onclick="() => ExecuteAction(ActionReplace)" class="px-3 py-2 bg-blue-600 text-white text-sm rounded-lg hover:bg-blue-700 transition">
                                🔁 Remplacer par nouvelle puce
                            </button>
                        }
                        @if (selectedChip.Status != "ARCHIVEE")
                        {
                            <button @onclick="OpenDeleteConfirmModal" class="px-3 py-2 bg-red-600 text-white text-sm rounded-lg hover:bg-red-700 transition">
                                🗑️ Supprimer
                            </button>
                        }
                    </div>
                </div>

                <!-- Historique des changements d'état -->
                @if (selectedChip.StatusHistory != null && selectedChip.StatusHistory.Count > 0)
                {
                    <div class="bg-gray-50 rounded-lg p-4 mb-6">
                        <h4 class="text-sm font-semibold text-gray-700 mb-3">Historique des changements</h4>
                        <div class="space-y-2 max-h-48 overflow-y-auto">
                            @foreach (var change in selectedChip.StatusHistory.OrderByDescending(x => x.ChangedAt))
                            {
                                <div class="flex items-start justify-between text-sm border-l-2 border-blue-300 pl-3 py-1">
                                    <div>
                                        <span class="@GetStatusBadgeClass(change.Status)">
                                            @GetStatusLabel(change.Status)
                                        </span>
                                        @if (!string.IsNullOrEmpty(change.Reason))
                                        {
                                            <p class="text-xs text-gray-600 mt-1">@change.Reason</p>
                                        }
                                    </div>
                                    <span class="text-xs text-gray-500 whitespace-nowrap ml-2">
                                        @change.ChangedAt.ToString("dd/MM/yyyy HH:mm:ss")
                                    </span>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
            <div class="bg-gray-50 border-t border-gray-200 p-6 flex justify-between">
                <div>
                    @if (selectedChip.Status != "ARCHIVEE")
                    {
                        <button @onclick="OpenDeleteConfirmModal" class="px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition">
                            🗑️ Supprimer
                        </button>
                    }
                </div>
                <button @onclick="CloseDetailModal" class="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">
                    Fermer
                </button>
            </div>
        </div>
    </div>
}

<!-- Modal Confirmation Suppression -->
@if (showDeleteConfirmModal)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 p-4" @onclick="CloseDeleteConfirmModal">
        <div class="bg-white rounded-xl shadow-2xl max-w-md w-full" @onclick:stopPropagation>
            <div class="bg-red-50 border-b border-red-200 p-6">
                <h3 class="text-2xl font-bold text-gray-900">Confirmation de suppression</h3>
            </div>
            <div class="p-6">
                <p class="text-gray-700 mb-4">@deleteConfirmMessage</p>
                <p class="text-sm text-gray-600">Cette action archivera la puce et la retirera du stock.</p>
            </div>
            <div class="bg-gray-50 border-t border-gray-200 p-6 flex justify-end gap-3">
                <button @onclick="CloseDeleteConfirmModal" class="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">
                    Annuler
                </button>
                <button @onclick="ConfirmDelete" class="px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition">
                    J'ai compris
                </button>
            </div>
        </div>
    </div>
}

@code {
    // Constantes pour les actions
    private const string ActionReceiveFromSupplier = "receive-from-supplier";
    private const string ActionEncode = "encode";
    private const string ActionShipToClient = "ship-to-client";
    private const string ActionConfirmDelivery = "confirm-delivery";
    private const string ActionAssignToControlPoint = "assign-to-controlpoint";
    private const string ActionRequestSav = "request-sav";
    private const string ActionDeactivate = "deactivate";
    private const string ActionReceiveSav = "receive-sav";
    private const string ActionReplace = "replace";
    private const string ActionDelete = "delete";

    private List<RfidChipDto> allChips = new();
    private List<RfidChipDto> filteredChips = new();
    private bool isLoading = true;

    private string selectedStatus = "";
    private string searchUid = "";

    private bool showAddManualModal = false;
    private string manualUid = "";
    private string addManualError = "";
    private bool isAddingManual = false;

    private bool showDetailModal = false;
    private RfidChipDto? selectedChip = null;

    private bool showDeleteConfirmModal = false;
    private string deleteConfirmMessage = "";
    private Func<Task>? pendingDeleteAction = null;

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
    }

    private async Task LoadData()
    {
        isLoading = true;
        try
        {
            var token = await AuthService.GetTokenAsync();
            Http.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue("Bearer", token);

            var response = await Http.GetAsync("api/rfidchips");
            if (response.IsSuccessStatusCode)
            {
                allChips = await response.Content.ReadFromJsonAsync<List<RfidChipDto>>() ?? new();
                filteredChips = new List<RfidChipDto>(allChips);
            }
            else
            {
                await JS.InvokeVoidAsync("alert", $"Erreur: {response.StatusCode}");
            }
        }
        catch (Exception ex)
        {
            await JS.InvokeVoidAsync("alert", $"Erreur: {ex.Message}");
        }
        finally
        {
            isLoading = false;
        }
    }

    private void FilterData()
    {
        filteredChips = allChips.Where(c =>
        {
            if (!string.IsNullOrEmpty(selectedStatus) && c.Status != selectedStatus)
                return false;
            if (!string.IsNullOrEmpty(searchUid) && !c.Uid.Contains(searchUid, StringComparison.OrdinalIgnoreCase))
                return false;
            return true;
        }).ToList();
    }

    private void ClearFilters()
    {
        selectedStatus = "";
        searchUid = "";
        FilterData();
    }

    private void NavigateToDashboard()
    {
        Navigation.NavigateTo("/admin-lc/dashboard");
    }

    private void OpenAddManualModal()
    {
        showAddManualModal = true;
        manualUid = "";
        addManualError = "";
    }

    private void CloseAddManualModal()
    {
        showAddManualModal = false;
        manualUid = "";
        addManualError = "";
    }

    private async Task HandleManualUidKeyUp(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await AddManualChip();
        }
    }

    private async Task AddManualChip()
    {
        if (string.IsNullOrWhiteSpace(manualUid))
        {
            addManualError = "Veuillez entrer un UID";
            return;
        }

        isAddingManual = true;
        addManualError = "";

        try
        {
            var token = await AuthService.GetTokenAsync();
            Http.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue("Bearer", token);

            var request = new { uid = manualUid };
            var response = await Http.PostAsJsonAsync("api/rfidchips/register-single", request);

            if (response.IsSuccessStatusCode)
            {
                await JS.InvokeVoidAsync("showNotification", "Puce ajoutée avec succès");
                CloseAddManualModal();
                await LoadData();
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                addManualError = $"Erreur: {response.StatusCode} - {errorContent}";
            }
        }
        catch (Exception ex)
        {
            addManualError = $"Erreur: {ex.Message}";
        }
        finally
        {
            isAddingManual = false;
        }
    }

    private void ViewChipDetails(Guid chipId)
    {
        selectedChip = allChips.FirstOrDefault(c => c.Id == chipId);
        if (selectedChip != null)
        {
            showDetailModal = true;
        }
    }

    private void CloseDetailModal()
    {
        showDetailModal = false;
        selectedChip = null;
    }

    private void OpenDeleteConfirmModal()
    {
        if (selectedChip == null) return;
        deleteConfirmMessage = $"Êtes-vous sûr de vouloir supprimer la puce {selectedChip.ChipId} ?";
        showDeleteConfirmModal = true;
    }

    private void CloseDeleteConfirmModal()
    {
        showDeleteConfirmModal = false;
        deleteConfirmMessage = "";
        pendingDeleteAction = null;
    }

    private async Task ConfirmDelete()
    {
        if (selectedChip == null) return;

        try
        {
            var token = await AuthService.GetTokenAsync();
            Http.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue("Bearer", token);

            var response = await Http.DeleteAsync($"api/rfidchips/{selectedChip.Id}");

            if (response.IsSuccessStatusCode)
            {
                await JS.InvokeVoidAsync("showNotification", "Puce archivée avec succès");
                CloseDeleteConfirmModal();
                CloseDetailModal();
                await LoadData();
            }
            else
            {
                await JS.InvokeVoidAsync("alert", $"Erreur: {response.StatusCode}");
            }
        }
        catch (Exception ex)
        {
            await JS.InvokeVoidAsync("alert", $"Erreur: {ex.Message}");
        }
    }

    private async Task ExecuteAction(string action)
    {
        if (selectedChip == null) return;

        try
        {
            var token = await AuthService.GetTokenAsync();
            Http.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue("Bearer", token);

            HttpResponseMessage response = null;

            switch (action)
            {
                case "receive-from-supplier":
                    response = await Http.PutAsJsonAsync($"api/rfidchips/{selectedChip.Id}/receive-from-supplier", new { supplierOrderId = "AUTO" });
                    break;
                case "encode":
                    response = await Http.PutAsync($"api/rfidchips/{selectedChip.Id}/encode", null);
                    break;
                case "ship-to-client":
                    response = await Http.PutAsJsonAsync($"api/rfidchips/{selectedChip.Id}/ship-to-client", new { clientOrderId = "AUTO" });
                    break;
                case "confirm-delivery":
                    response = await Http.PutAsJsonAsync($"api/rfidchips/{selectedChip.Id}/confirm-delivery", new { packagingCode = selectedChip.PackagingCode ?? "" });
                    break;
                case "assign-to-controlpoint":
                    var cpId = await JS.InvokeAsync<string>("prompt", "ID du point de contrôle:");
                    if (!string.IsNullOrEmpty(cpId) && Guid.TryParse(cpId, out var cpGuid))
                    {
                        response = await Http.PutAsJsonAsync($"api/rfidchips/{selectedChip.Id}/assign-to-controlpoint", new { controlPointId = cpGuid });
                    }
                    break;
                case "request-sav":
                    var reason = await JS.InvokeAsync<string>("prompt", "Raison du SAV:");
                    if (!string.IsNullOrEmpty(reason))
                    {
                        response = await Http.PostAsJsonAsync($"api/rfidchips/{selectedChip.Id}/request-sav", new { reason });
                    }
                    break;
                case "receive-sav":
                    response = await Http.PutAsync($"api/rfidchips/{selectedChip.Id}/receive-sav", null);
                    break;
                case "replace":
                    var newChipId = await JS.InvokeAsync<string>("prompt", "ID de la nouvelle puce:");
                    if (!string.IsNullOrEmpty(newChipId) && Guid.TryParse(newChipId, out var newGuid))
                    {
                        response = await Http.PutAsJsonAsync($"api/rfidchips/{selectedChip.Id}/replace", new { replacementChipId = newGuid });
                    }
                    break;
                case "deactivate":
                case "delete":
                    response = await Http.DeleteAsync($"api/rfidchips/{selectedChip.Id}");
                    break;
            }

            if (response != null && response.IsSuccessStatusCode)
            {
                await JS.InvokeVoidAsync("showNotification", "Action exécutée avec succès");
                CloseDetailModal();
                await LoadData();
            }
            else if (response != null)
            {
                await JS.InvokeVoidAsync("alert", $"Erreur: {response.StatusCode}");
            }
        }
        catch (Exception ex)
        {
            await JS.InvokeVoidAsync("alert", $"Erreur: {ex.Message}");
        }
    }

    private async Task RefreshData()
    {
        await LoadData();
    }

    private string GetStatusBadgeClass(string status)
    {
        return status switch
        {
            "EN_TRANSIT" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-50 text-yellow-800 border border-yellow-200",
            "EN_ATELIER" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-orange-50 text-orange-800 border border-orange-200",
            "EN_STOCK" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-50 text-blue-800 border border-blue-200",
            "EN_LIVRAISON" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-purple-50 text-purple-800 border border-purple-200",
            "LIVREE" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-cyan-50 text-cyan-800 border border-cyan-200",
            "ACTIVE" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-50 text-green-800 border border-green-200",
            "RETOUR_SAV" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-red-50 text-red-800 border border-red-200",
            "RECEPTION_SAV" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-pink-50 text-pink-800 border border-pink-200",
            "REMPLACEE" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-indigo-50 text-indigo-800 border border-indigo-200",
            "ARCHIVEE" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-slate-50 text-slate-800 border border-slate-200",
            "INACTIVE" => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gray-50 text-gray-800 border border-gray-200",
            _ => "inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gray-50 text-gray-800 border border-gray-200"
        };
    }

    private string GetStatusLabel(string status)
    {
        return status switch
        {
            "EN_TRANSIT" => "🚚 En transit",
            "EN_ATELIER" => "🔧 En atelier",
            "EN_STOCK" => "📦 En stock",
            "EN_LIVRAISON" => "🚛 En livraison",
            "LIVREE" => "✅ Livrée",
            "ACTIVE" => "🟢 Active",
            "RETOUR_SAV" => "🔄 Retour SAV",
            "RECEPTION_SAV" => "📥 Réception SAV",
            "REMPLACEE" => "🔁 Remplacée",
            "ARCHIVEE" => "📁 Archivée",
            "INACTIVE" => "⚫ Inactive",
            _ => status
        };
    }
}
