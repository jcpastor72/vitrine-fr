@page "/admin-lc/collaborators"
@layout AdminLayout
@using LaborControl.Web.Models
@using LaborControl.Web.Services
@inject NavigationManager Navigation

<div class="space-y-6">
    <!-- Header -->
    <div class="flex justify-between items-center">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">Gestion des Collaborateurs</h1>
            <p class="text-gray-600 mt-2">Gérez les membres de votre équipe Labor Control</p>
        </div>
        <button @onclick="CreateNewCollaborator" class="px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 transition font-medium">
            ➕ Ajouter un collaborateur
        </button>
    </div>

    <!-- Statistics by Role -->
    <div class="grid md:grid-cols-5 gap-4">
        <div class="bg-white rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-gray-900">@totalUsers</div>
            <div class="text-sm text-gray-600">Total</div>
        </div>
        <div class="bg-blue-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-blue-700">@adminCount</div>
            <div class="text-sm text-blue-600">Administrateurs</div>
        </div>
        <div class="bg-purple-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-purple-700">@managerCount</div>
            <div class="text-sm text-purple-600">Managers</div>
        </div>
        <div class="bg-orange-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-orange-700">@supportCount</div>
            <div class="text-sm text-orange-600">Support</div>
        </div>
        <div class="bg-green-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-green-700">@logisticsCount</div>
            <div class="text-sm text-green-600">Logistique</div>
        </div>
    </div>

    <!-- Collaborators Table -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gray-50 border-b-2 border-gray-200">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Nom</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Email</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Rôle</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Statut</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Dernière connexion</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <tr class="hover:bg-gray-50 transition cursor-pointer" @onclick="() => ViewCollaboratorDetails(1)">
                        <td class="px-6 py-4 font-medium text-gray-900">Jean Dupont</td>
                        <td class="px-6 py-4 text-sm text-blue-600">jean@laborcontrol.fr</td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">👑 Admin</span>
                        </td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">Actif</span>
                        </td>
                        <td class="px-6 py-4 text-sm text-gray-600">Aujourd'hui - 10:30</td>
                    </tr>
                    <tr class="hover:bg-gray-50 transition cursor-pointer" @onclick="() => ViewCollaboratorDetails(2)">
                        <td class="px-6 py-4 font-medium text-gray-900">Marie Martin</td>
                        <td class="px-6 py-4 text-sm text-blue-600">marie@laborcontrol.fr</td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-purple-100 text-purple-800">📊 Manager</span>
                        </td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">Actif</span>
                        </td>
                        <td class="px-6 py-4 text-sm text-gray-600">Hier - 16:45</td>
                    </tr>
                    <tr class="hover:bg-gray-50 transition cursor-pointer" @onclick="() => ViewCollaboratorDetails(3)">
                        <td class="px-6 py-4 font-medium text-gray-900">Pierre Bernard</td>
                        <td class="px-6 py-4 text-sm text-blue-600">pierre@laborcontrol.fr</td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-orange-100 text-orange-800">💬 Support</span>
                        </td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">Actif</span>
                        </td>
                        <td class="px-6 py-4 text-sm text-gray-600">Il y a 2 jours</td>
                    </tr>
                    <tr class="hover:bg-gray-50 transition cursor-pointer" @onclick="() => ViewCollaboratorDetails(4)">
                        <td class="px-6 py-4 font-medium text-gray-900">Sophie Leclerc</td>
                        <td class="px-6 py-4 text-sm text-blue-600">sophie@laborcontrol.fr</td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">📦 Logistique</span>
                        </td>
                        <td class="px-6 py-4">
                            <span class="inline-flex px-3 py-1 rounded-full text-xs font-semibold bg-red-100 text-red-800">Inactif</span>
                        </td>
                        <td class="px-6 py-4 text-sm text-gray-600">Il y a 1 mois</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Role Legend -->
    <div class="bg-gray-50 rounded-lg shadow p-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">📖 Guide des rôles</h3>
        <div class="grid md:grid-cols-4 gap-4">
            <div class="bg-white rounded-lg p-4">
                <p class="font-bold text-blue-900 mb-2">👑 Administrateur</p>
                <p class="text-sm text-gray-600">Accès complet, gestion utilisateurs, configuration système</p>
            </div>
            <div class="bg-white rounded-lg p-4">
                <p class="font-bold text-purple-900 mb-2">📊 Manager</p>
                <p class="text-sm text-gray-600">Gestion clients, commandes, rapports</p>
            </div>
            <div class="bg-white rounded-lg p-4">
                <p class="font-bold text-orange-900 mb-2">💬 Support</p>
                <p class="text-sm text-gray-600">Support client, fiches clients, réinitialisation MDP</p>
            </div>
            <div class="bg-white rounded-lg p-4">
                <p class="font-bold text-green-900 mb-2">📦 Logistique</p>
                <p class="text-sm text-gray-600">Préparation, expéditions, suivi puces RFID</p>
            </div>
        </div>
    </div>
</div>

@code {
    private int totalUsers = 4;
    private int adminCount = 1;
    private int managerCount = 1;
    private int supportCount = 1;
    private int logisticsCount = 1;

    private void CreateNewCollaborator()
    {
        Navigation.NavigateTo("/admin-lc/collaborators/new");
    }

    private void EditCollaborator(int id)
    {
        Navigation.NavigateTo($"/admin-lc/collaborators/{id}/edit");
    }

    private void ViewCollaboratorDetails(int id)
    {
        Navigation.NavigateTo($"/admin-lc/collaborators/{id}");
    }
}
