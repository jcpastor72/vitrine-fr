@page "/admin-lc/preparation"
@layout AdminLayout
@using VitrineFr.Models
@using VitrineFr.Services
@inject NavigationManager Navigation

<div class="space-y-6">
    <!-- Header -->
    <div class="flex justify-between items-center">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">Préparation de Commandes</h1>
            <p class="text-gray-600 mt-2">Préparez et organisez les commandes pour l'expédition</p>
        </div>
    </div>

    <!-- Statistics -->
    <div class="grid md:grid-cols-4 gap-4">
        <div class="bg-yellow-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-yellow-700">@toBePrepped</div>
            <div class="text-sm text-yellow-600">À préparer</div>
        </div>
        <div class="bg-blue-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-blue-700">@inPreparation</div>
            <div class="text-sm text-blue-600">En préparation</div>
        </div>
        <div class="bg-purple-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-purple-700">@readyToShip</div>
            <div class="text-sm text-purple-600">Prêtes à expédier</div>
        </div>
        <div class="bg-gray-50 rounded-lg shadow p-4">
            <div class="text-2xl font-bold text-gray-700">@todayPrepared</div>
            <div class="text-sm text-gray-600">Préparées aujourd'hui</div>
        </div>
    </div>

    <!-- Preparation Queue -->
    <div class="grid md:grid-cols-2 gap-6">
        <!-- To Be Prepared -->
        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">⏱️ À préparer</h3>
            <div class="space-y-3">
                <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded">
                    <div class="flex justify-between items-start">
                        <div>
                            <p class="font-bold text-gray-900">#CMD-2025-003</p>
                            <p class="text-sm text-gray-600">Société DEF</p>
                            <p class="text-xs text-gray-500 mt-1">📦 Quantité: 5 unités</p>
                        </div>
                        <button class="px-3 py-1 bg-blue-600 text-white rounded text-sm hover:bg-blue-700 font-medium">
                            Commencer
                        </button>
                    </div>
                </div>
                <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded">
                    <div class="flex justify-between items-start">
                        <div>
                            <p class="font-bold text-gray-900">#CMD-2025-004</p>
                            <p class="text-sm text-gray-600">Entreprise XYZ</p>
                            <p class="text-xs text-gray-500 mt-1">📦 Quantité: 8 unités</p>
                        </div>
                        <button class="px-3 py-1 bg-blue-600 text-white rounded text-sm hover:bg-blue-700 font-medium">
                            Commencer
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- In Preparation -->
        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-bold text-gray-900 mb-4">🏗️ En préparation</h3>
            <div class="space-y-3">
                <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
                    <div class="flex justify-between items-start mb-2">
                        <div>
                            <p class="font-bold text-gray-900">#CMD-2025-002</p>
                            <p class="text-sm text-gray-600">Groupe XYZ</p>
                        </div>
                        <span class="text-xs bg-blue-100 text-blue-800 px-2 py-1 rounded">40%</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-2">
                        <div class="bg-blue-600 h-2 rounded-full" style="width: 40%"></div>
                    </div>
                    <div class="flex justify-between items-center mt-3">
                        <p class="text-xs text-gray-600">Sophie Leclerc</p>
                        <button class="px-3 py-1 bg-green-600 text-white rounded text-sm hover:bg-green-700 font-medium">
                            Terminer
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Ready to Ship -->
    <div class="bg-white rounded-lg shadow p-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">✓ Prêtes à expédier</h3>
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Commande</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Client</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Préparée par</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700">Date</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <tr class="hover:bg-gray-50 transition cursor-pointer" @onclick="() => ViewPreparationDetails(1)">
                        <td class="px-6 py-4 font-mono font-semibold text-blue-600">#CMD-2025-001</td>
                        <td class="px-6 py-4 font-medium text-gray-900">Entreprise ABC</td>
                        <td class="px-6 py-4 text-sm text-gray-600">Marie Martin</td>
                        <td class="px-6 py-4 text-sm text-gray-600">22/10/2025</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Quality Check -->
    <div class="bg-green-50 rounded-lg shadow p-6 border-l-4 border-green-600">
        <h3 class="text-lg font-bold text-green-900 mb-3">✓ Contrôle qualité</h3>
        <p class="text-sm text-green-800">Toutes les commandes prêtes à expédier ont été vérifiées et sont conformes aux normes de qualité.</p>
    </div>
</div>

@code {
    private int toBePrepped = 2;
    private int inPreparation = 1;
    private int readyToShip = 1;
    private int todayPrepared = 3;

    private void ViewPreparationDetails(int id)
    {
        Navigation.NavigateTo($"/admin-lc/preparation/{id}");
    }
}
